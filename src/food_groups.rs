#[derive(Clone, Debug)]
pub(crate) enum Carbs {
    CousCous,
    Pasta,
    Potato,
    Rice,
}

#[derive(Clone, Debug)]
pub(crate) enum Protein {
    Beef,
    Chicken,
    Fish,
    Pork,
    Tofu,
}