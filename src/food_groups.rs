#[derive(Clone, Debug, PartialEq)]
pub(crate) enum Carbs {
    CousCous,
    Pasta,
    Potato,
    Rice,
}

#[derive(Clone, Debug, PartialEq)]
pub(crate) enum Protein {
    Beef,
    Chicken,
    Fish,
    Pork,
    Tofu,
}